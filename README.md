# Hello!

For installing eslint in the project please run these commands (use **npm** or **yarn**):

`yarn add --dev eslint`

`yarn run eslint --init (AIRBNB CONFIG)`

`yarn add --dev prettier eslint-config-prettier eslint-plugin-prettier eslint-import-resolver-typescript`
